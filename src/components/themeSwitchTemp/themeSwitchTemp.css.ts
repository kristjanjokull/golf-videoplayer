import { style } from "@vanilla-extract/css";

export const themeButton = style({
  position: "absolute",
  top: 10,
  left: 10,
  width: 60,
  height: 40,
  zIndex: 2,
  cursor: "pointer",
  border: "none",
});
